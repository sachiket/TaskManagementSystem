apiVersion: v1
kind: Service
metadata:
  name: task-management-service-node-port
spec:
  type: NodePort
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 31515
  selector:
    component: web
